<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!--
            Los tests corren contra MySQL, no contra SQLite en memoria, y es a propósito.

            Las dos defensas centrales del proyecto son específicas de InnoDB: la columna
            generada con índice único y el bloqueo de SELECT ... FOR UPDATE con gap locks.
            En SQLite `lockForUpdate()` es un no-op y no hay forma de abrir dos conexiones
            concurrentes contra una base `:memory:`, así que el test de concurrencia no
            probaría nada. Testear sobre el mismo motor que corre en producción es la
            única manera de que estos tests signifiquen algo.

            La base `reservas_testing` se crea sola con `php artisan test` si existe el
            contenedor; ver el README.
        -->
        <env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_HOST" value="127.0.0.1"/>
        <env name="DB_PORT" value="3309"/>
        <env name="DB_DATABASE" value="reservas_testing"/>
        <env name="DB_USERNAME" value="reservas"/>
        <env name="DB_PASSWORD" value="reservas"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
